/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Check if there are any redirects or rewrites here that might affect /create-password
}

module.exports = nextConfig

